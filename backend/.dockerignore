# Docker 이미지를 빌드할 때 이 파일들은 빌드 컨텍스트(Dockerfile이 COPY할 수 있는 범위)에서
# 제외합니다. 안 넣으면 로컬에서 이미 만들어진 build/ 산출물이나 비밀값이 든 .env가 그대로
# 이미지 빌드 과정에 딸려 들어가서, 빌드가 느려지거나(불필요한 대용량 폴더) 이미지 안에
# 비밀값이 남는 사고로 이어질 수 있습니다.
build/
.gradle/
.env
*.log
